export const FRONTEND_VERSION = '4.0.1'; // Update this constant to change displayed frontend version
export const DEFAULT_BACKEND_VERSION = '4.0.1'; // Fallback when backend /version is unavailable
export const DEFAULT_MONITOR_VERSION = 'N/A'; // Fallback when no monitor version is available
